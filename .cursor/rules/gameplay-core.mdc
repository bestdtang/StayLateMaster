---
description: 核心玩法系统边界、数值规则与实现约束
alwaysApply: true
---

# 核心玩法 — 系统与规则

## 两轴竞争

| 数值 | 规则 |
|------|------|
| **快乐** 0→100 | 右滑感兴趣内容、火热时间刷内容；满 100 胜利 |
| **疲劳** 0→100 | 随时间曲线加速增长，**不会下降**；满 100 失败 |

眨眼只能**暂时下调**疲劳增速；完全失败（红圈按键或判定区缩尽）则**暂时上调**。具体倍率见配置（默认 成功 ×0.6 / 失败 ×1.75），判定区未缩尽前不按不触发眨眼惩罚。

疲劳增速修饰按**来源**管理：同一来源（眨眼三态共用 `Blink`）在任意时刻只保留一个，**最新一次覆盖**上一次；不同来源之间才相乘叠加。因此连续失败只维持 ×2，不会 ×2→×4→×8 螺旋暴毙。

**超级疲劳（CrazyFatigue）**：连续 3 次完全失败进入；额外疲劳增速（配置 `CrazyFatigueRateMultiplier`，与 Blink 相乘）+ 钟摆周期略快（`CrazyFatiguePendulumPeriodMultiplier`）；**1 次**绿圈成功或黄圈偏早/偏晚解除。

## 两大玩法（并行、注意力冲突）

### 1. 钟摆眨眼 — `Pendulum/`

- 钟摆持续来回摆动；判定区在两次眨眼之间缩小，按键后刷新回满
- 绿圈成功：自然眨眼，疲劳增速临时下调（默认 ×0.6）
- 黄圈普通失误：疲劳增速临时略降（默认 ×0.9）
- 红圈按键 / 判定区缩至 0 未按：完全失败，疲劳增速临时上调（默认 ×1.75）
- 判定区未缩尽且未按：无眨眼判定
- 倍率跨度别拉太大，否则简单阶段过慢、困难阶段过快，节奏失衡
- 后期分段（阈值 `ZonePhase2AtFatigue`/`ZonePhase3AtFatigue` 配置驱动，为节奏可调、不必等于视觉分段）：指针加速；判定区 一阶段居中 → 二阶段反侧随机刷新且满宽缩小 → 三阶段不规则加速度摆动（非漂移）；中心角 + 外圈半宽不超过轨道最大摆角；节奏目标是三阶段占进度条最多、一阶段简短、二阶段不拖沓
- 第三阶段（不规则摆动）须保持**可反应、可眨眼**：摆动幅度/频率与指针提速适度，不做到几乎必然连续失败

### 2. 手机筛选 — `Phone/`

- 卡片带 **Topic**（MVP 5 类：萌宠、美女/帅哥、美食、汽车、足球）
- **右滑**=喜欢，**左滑**=跳过
- 头顶 **兴趣气泡** 表示当前最想看的 Topic，约 15–30s 切换；切换前可预告
- 正确：兴趣+右滑 → 加快乐、提该 Topic 权重；非兴趣+左滑 → 维持连击、降权重
- 错误：兴趣+左滑 / 非兴趣+右滑 → 断连击、小幅减快乐、扭曲推荐权重
- 惩罚来自**错误操作**，不是卡片单纯出现

## 推荐流 — `Recommendation/`

- 连续右滑某 Topic → 更常出现；连续左滑 → 减少
- 抽卡权重 = 训练权重 × 当前兴趣亲和倍率（`interestAffinity`），当前兴趣明显占主导
- **硬保底**：连续 `interestGuaranteeGap` 张未出当前兴趣 → 下一张强制当前兴趣，避免干等
- 任意 Topic 仍可能再现；兴趣切换**不清空**旧权重，新兴趣需靠滑动逐渐训练出来
- 这是右侧主要策略感，实现时权重要可感知、可调

## 火热时间 — `HotStreak/`

- 短时间连续正确筛选 + 累计快乐 → 触发（~3s）
- 期间：内容快速刷新，每篇固定加快乐，**无需判兴趣**，**不改**长期推荐权重
- 钟摆眨眼与疲劳增速修饰**照常进行**（与滑卡并行）
- 结束后恢复；可在结束时切换兴趣

## 建议脚本划分

```
Scripts/
  Core/           RunController, GameState, WinLose
  Stats/          HappinessMeter, FatigueMeter
  Pendulum/       PendulumController, BlinkInput
  Phone/          CardView, SwipeInput, FeedSpawner
  Interest/       InterestManager
  Recommendation/ TopicWeights, CardPicker
  HotStreak/      HotTimeController
  Visual/         FatigueVisuals, CharacterExpression
  Audio/          AudioManager, RunAudioController, GameAudioConfig
  UI/             HUD, ResultsScreen, TutorialHints
  Data/           TopicId, CardSpriteLibrary, PickedCard
```

跨系统通信用 `RunController` 或少量 C# event；避免各系统互相 `GetComponent` 链式依赖。

## 验收锚点（实现时对照 §19）

1. 数秒内懂左右滑 + 眨眼
2. 滑动能明显改变后续内容
3. 兴趣切换后新 Topic 不会长期刷不出
4. 高疲劳影响视觉但不至于完全无法操作
5. 一局紧凑，可快速重开
